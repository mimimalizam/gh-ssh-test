version: v1.0
name: First pipeline example
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: "Bundle install"
    task:
      secrets:
        # Mount the secret:
        - name: private-repo
      jobs:
      - name: Fetch gems
        commands:
          - chmod 0600 ~/.keys/*
          # Add the key to the ssh agent:
          - ssh-add ~/.keys/*
          - bundler --version
          - gem install --force bundler:1.17.3
          - bundler --version
  - name: "Checking parallelism"
    task:
      jobs:
      - name: Another parallelism
        parallelism: 3
        commands:
          - env | grep SEMAPHORE
      - name: Job outside of parallelism
        commands:
          - env
      - name: Explore env
        parallelism: 6
        commands:
          - env
          - env | grep SEMAPHORE
